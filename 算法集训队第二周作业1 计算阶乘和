#include <stdio.h>
int fact(int x){
    int sum=1;
    for (int i = 1; i <=x ; ++i) {
        sum*=i;
    }
    return sum;
}
int main(){
    int n;
    scanf("%d",&n);
    int sum=0;
    for (int i = 1; i <=n ; ++i) {
        sum+= fact(i);
    }
    printf("%d",sum);
    return 0;
}
