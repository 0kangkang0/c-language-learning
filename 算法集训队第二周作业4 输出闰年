#include<stdio.h>
int main()
{
    int year;
    scanf("%d",&year);
    if(year>=2000 && year<=2100){
    for(int y=2000;y<=year;y++){
        if(y%4==0 && y%100!=0){
            printf("%d\n",y);
        }else{}
    }
    }else{
        printf("Invalid year!");
    }
    if(year<2004 && year>=2000){
        printf("None");
    }
    return 0;
}
